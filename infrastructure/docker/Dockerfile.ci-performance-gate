FROM python:3.11-slim

ENV DEBIAN_FRONTEND=noninteractive \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_NO_CACHE_DIR=1

# Minimal prebuilt image for the performance gate.
# This keeps the gate fast and avoids repeated base-image cold starts in Woodpecker.
RUN python -m pip install --upgrade pip
