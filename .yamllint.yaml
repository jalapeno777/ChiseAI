extends: default

rules:
  document-start: disable

  line-length:
    max: 140
    level: warning

  indentation:
    spaces: 2
    indent-sequences: consistent

  truthy: disable
  comments: disable
