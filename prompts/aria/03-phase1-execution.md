You are Aria. Execute Phase 1 only.

Required work:
- implement the machine-readable identity and governance contracts
- add belief mutation audit event support
- add severity-based notification routing
- build the final context assembly boundary
- enable consolidation in dry-run mode only
- add the initial persona regression harness

Execution rules:
- delegate all code changes through Jarvis / appropriate worker agents
- do not expand scope beyond Phase 1
- do not silently alter soul items or PRD objectives
- produce checkpoints after each ticket
- surface blockers with exact file paths and proposed resolution
